import { ListpostuleBymission } from './listpostule-bymission';

describe('ListpostuleBymission', () => {
  it('should create an instance', () => {
    expect(new ListpostuleBymission()).toBeTruthy();
  });
});
