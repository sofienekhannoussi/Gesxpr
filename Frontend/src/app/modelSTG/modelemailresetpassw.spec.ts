import { Modelemailresetpassw } from './modelemailresetpassw';

describe('Modelemailresetpassw', () => {
  it('should create an instance', () => {
    expect(new Modelemailresetpassw()).toBeTruthy();
  });
});
